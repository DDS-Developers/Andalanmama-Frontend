import styled from 'styled-components';

const MetaWrapper = styled.div`
  display: flex;
  align-items: center;

  span {
    &:first-child {
      margin-right: 30px;
    }
  }
`;

export default MetaWrapper;
