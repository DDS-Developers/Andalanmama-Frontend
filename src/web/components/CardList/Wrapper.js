import styled from 'styled-components';
import Box from '../Box';

const Wrapper = styled(Box)`
  position: relative;
  display: flex;
  align-items: center;
  flex-wrap: wrap;
  margin: 0 -10px;
`;

export default Wrapper;
