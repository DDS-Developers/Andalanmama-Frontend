import styled from 'styled-components';

const InfoWrapper = styled.div`
  display: flex;
  align-items: center;

  span {
    color: #ffffff;

    &:first-child {
      margin-right: 30px;
    }
  }
`;

export default InfoWrapper;
