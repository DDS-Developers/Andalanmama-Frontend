/* eslint-disable indent */
import styled from 'styled-components';

const inner = styled.div`
  position: relative;
  height: 100%;
  overflow: hidden;
  border-radius: 5px;
`;

export default inner;
