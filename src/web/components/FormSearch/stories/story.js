import React from 'react';
import { storiesOf } from '@storybook/react';

import FormSearch from '../Layout';

storiesOf('FormSearch', module).add('Form Search', () => (
  <FormSearch backgroundColor="#dddddd" padding="20px" />
));
