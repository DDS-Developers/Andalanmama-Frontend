import styled from 'styled-components';

const ImageWrapper = styled.div`
  width: 160px;
  flex: 0 0 100px;

  img {
    display: block;
    width: 100%;
    border-radius: 100%;
  }
`;

export default ImageWrapper;
