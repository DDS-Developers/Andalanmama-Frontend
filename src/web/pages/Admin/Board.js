import React from 'react';

const Dashboard = () => (
  <div className="container">
    <div className="row">
      <div className="col-6">
        <p>
          Include Bootstrap’s source Sass and JavaScript files via npm, Composer or Meteor. Package
          managed installs don’t include documentation, but do include our build system and readme.
        </p>
      </div>
    </div>
  </div>
);

export default Dashboard;
