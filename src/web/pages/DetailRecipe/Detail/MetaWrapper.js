import styled from 'styled-components';

const MetaWrapper = styled.div`
  display: flex;
  align-items: center;
  margin-bottom: 50px;

  span {
    &:first-child {
      margin-right: 30px;
    }
  }
`;

export default MetaWrapper;
