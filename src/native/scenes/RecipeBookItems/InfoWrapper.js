/**
 * scenes/RecipeBookItems/InfoWrapper.js
 *
 */
import styled from 'styled-components';
import { View } from 'native-base';

const InfoWrapper = styled(View)`
  padding-left: 15px;
  padding-right: 15px;
  margin-bottom: 5px;
`;

export default InfoWrapper;
