/**
 * scenes/Search/List/ListWrapper.js
 *
 */
import styled from 'styled-components';
import { View } from 'native-base';

const ListWrapper = styled(View)`
  position: relative;
  flex: 1;
  margin-top: 20px;
`;

export default ListWrapper;
