/**
 * scenes/Search/List/LoaderWrapper.js
 *
 */
import styled from 'styled-components';
import { View } from 'native-base';

const LoaderWrapper = styled(View)`
  position: relative;
  min-height: 160px;
  align-items: center;
  justify-content: center;
`;

export default LoaderWrapper;
