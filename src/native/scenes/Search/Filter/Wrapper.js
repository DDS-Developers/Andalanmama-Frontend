/**
 * scenes/Search/Filter/Wrapper.js
 *
 */
import styled from 'styled-components';
import { View } from 'native-base';

const Wrapper = styled(View)`
  flex-direction: row;
  align-items: center;
  justify-content: space-between;
  height: 48px;
  background-color: #ffffff;
  z-index: -1;
`;

export default Wrapper;
