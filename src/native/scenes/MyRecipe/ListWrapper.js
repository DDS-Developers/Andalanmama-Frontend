/**
 * scenes/MyRecipe/ListWrapper.js
 *
 */
import styled from 'styled-components';
import { View } from 'native-base';

const ListWrapper = styled(View)`
  flex-direction: row;
  flex-wrap: wrap;
  padding-top: 20px;
  padding-bottom: 20px;
  padding-left: 15px;
  padding-right: 15px;
`;

export default ListWrapper;
