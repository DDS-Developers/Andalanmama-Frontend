/**
 * scenes/Login/ButtonIcon.js
 *
 */
import styled from 'styled-components';
import { Button } from 'native-base';

const ButtonIcon = styled(Button)`
  background-color: #ffffff;
  border-radius: 4px;
  justify-content: flex-start;
  margin-bottom: 13px;
`;

export default ButtonIcon;
