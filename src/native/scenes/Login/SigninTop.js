/**
 * scenes/Login/SigninTop.js
 *
 */
import styled from 'styled-components';
import { View } from 'native-base';

const SigninTop = styled(View)`
  margin: 15px -15px 0;
  justify-content: flex-start;
  align-items: center;
`;

export default SigninTop;
