/**
 * scenes/Register/BottomWrapper.js
 *
 */
import styled from 'styled-components';
import { View } from 'react-native';

const Wrapper = styled(View)`
  position: relative;
  background-color: #000000;
  margin-top: -56;
  z-index: -1;
  height: 288;
`;

export default Wrapper;
