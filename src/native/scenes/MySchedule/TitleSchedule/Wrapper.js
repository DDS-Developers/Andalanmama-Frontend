/**
 * scenes/MySchedule/TitleSchedule/ScheduleWrapper.js
 *
 */
import styled from 'styled-components';
import { View } from 'react-native';

const ScheduleWrapper = styled(View)`
  flex-direction: row;
  align-items: center;
  margin-bottom: 20;
`;

export default ScheduleWrapper;
