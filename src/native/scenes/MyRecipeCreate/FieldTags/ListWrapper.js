/**
 * scenes/MyRecipeCreate/FieldTags/ListWrapper.js
 *
 */
import styled from 'styled-components';
import { View } from 'native-base';

const ListWrapper = styled(View)`
  margin-top: 20px;
  flex-direction: row;
  align-items: center;
  flex-wrap: wrap;
`;

export default ListWrapper;
