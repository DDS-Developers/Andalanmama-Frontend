/**
 * components/Header/BodyWrapper.js
 *
 */
import styled from 'styled-components';
import { Body } from 'native-base';

const BodyWrapper = styled(Body)`
  flex: 4;
  align-items: center;
  justify-content: center;
  position: relative;
`;

export default BodyWrapper;
