/**
 * scenes/ExploreFilter/Header/FormWrapper.js
 *
 */
import styled from 'styled-components';
import { Form } from 'native-base';

const FormWrapper = styled(Form)`
  height: 34;
  width: 100%;
  position: relative;
`;

export default FormWrapper;
