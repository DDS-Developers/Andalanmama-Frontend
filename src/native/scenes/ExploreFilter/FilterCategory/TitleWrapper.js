/**
 * scenes/Explore/FilterCategory/TitleWrapper.js
 *
 */
import styled from 'styled-components';
import { Body } from 'native-base';

const TitleWrapper = styled(Body)`
  flex: 2;
  align-items: center;
  justify-content: center;
`;

export default TitleWrapper;
