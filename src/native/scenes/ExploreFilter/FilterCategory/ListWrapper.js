/**
 * scenes/Explore/FilterCategory/ListWrapper.js
 *
 */
import styled from 'styled-components';
import { View } from 'native-base';

const ListWrapper = styled(View)`
  flex-direction: row;
  align-items: center;
  flex-wrap: wrap;
  justify-content: center;
  margin-left: -6px;
  margin-right: -6px;
  margin-bottom: 20px;
`;

export default ListWrapper;
