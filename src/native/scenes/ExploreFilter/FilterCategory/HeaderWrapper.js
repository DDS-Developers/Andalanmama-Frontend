/**
 * scenes/Explore/FilterCategory/HeaderWrapper.js
 *
 */
import styled from 'styled-components';
import { View } from 'native-base';

const HeaderWrapper = styled(View)`
  flex-direction: row;
  justify-content: center;
  elevation: 3;
`;

export default HeaderWrapper;
