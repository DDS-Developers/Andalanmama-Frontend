/**
 * components/Header/TitleWrapper.js
 *
 */
import styled from 'styled-components';
import { Title } from 'native-base';

const TitleWrapper = styled(Title)`
  color: #e83249;
`;

export default TitleWrapper;
