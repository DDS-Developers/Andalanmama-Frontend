/**
 * components/ButtonPrimary/TextWrapper.js
 *
 */
import styled from 'styled-components';
import { Text } from 'native-base';

const TextWrapper = styled(Text)`
  color: #ffffff;
  text-transform: capitalize;
  font-size: 14px;
  text-align: center;
`;

export default TextWrapper;
