/**
 * components/CardRecipe/Toggle.js
 *
 */
import styled from 'styled-components';
import { Button } from 'native-base';

const Toggle = styled(Button)`
  flex: 1;
  padding-bottom: 0;
  height: null;
  justify-content: flex-start;
  padding-top: 1;
  align-items: flex-start;
`;

export default Toggle;
