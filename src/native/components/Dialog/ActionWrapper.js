/**
 * components/Dialog/ActionWrapper.js
 *
 */
import styled from 'styled-components';
import { View } from 'react-native';

const ActionWrapper = styled(View)`
  align-items: center;
  flex-direction: row;
  justify-content: flex-end;
`;

export default ActionWrapper;
