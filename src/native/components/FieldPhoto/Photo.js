/**
 * components/FieldPhoto/Photo.js
 *
 */
import styled from 'styled-components';
import { Thumbnail } from 'native-base';

const Photo = styled(Thumbnail)`
  width: 140px;
  height: 140px;
  border-radius: 5px;
  overflow: hidden;
`;

export default Photo;
