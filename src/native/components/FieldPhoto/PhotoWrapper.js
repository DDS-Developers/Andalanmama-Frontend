/**
 * components/FieldPhoto/PhotoWrapper.js
 *
 */
import styled from 'styled-components';
import { View } from 'native-base';

const PhotoWrapper = styled(View)`
  width: 140px;
  height: 140px;
  position: relative;
`;

export default PhotoWrapper;
