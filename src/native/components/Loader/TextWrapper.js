/**
 * components/Loader/TextWrapper.js
 *
 */
import styled from 'styled-components';
import { Text } from 'native-base';

const TextWrapper = styled(Text)`
  color: #666666;
  margin-top: 10;
`;

export default TextWrapper;
